import 'package:flutter/material.dart';

class ClienteProfileView extends StatelessWidget {
  const ClienteProfileView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Perfil Cliente', style: TextStyle(fontSize: 30)),
    );
  }
}
